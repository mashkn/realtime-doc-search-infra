{
  "scripts": {
    "dev:api": "npm --prefix api run dev",
    "lint": "eslint \"{api,indexer,shared}/src/**/*.ts\"",
    "typecheck": "npm --prefix api run typecheck && npm --prefix indexer run typecheck && npm --prefix shared run typecheck",
    "prepare": "husky"
  },
  "devDependencies": {
    "@eslint/js": "^9.20.0",
    "eslint": "^9.20.0",
    "globals": "^15.15.0",
    "husky": "^9.1.7",
    "typescript-eslint": "^8.24.1"
  }
}
